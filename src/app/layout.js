import "./globals.css";

export const metadata = {
  title: "Paint Reveal",
  description: "",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
